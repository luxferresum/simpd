import parseMpd from '../../../utils/parse-mpd';
import { module, test } from 'qunit';

module('Unit | Utility | parse mpd');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = parseMpd();
  assert.ok(result);
});
